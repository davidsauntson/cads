<!doctype html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="{% webpack_path css %}" />
  </head>
  <body>
    {{ content }}

    <script src="{% webpack_path js %}" defer></script>
  </body>
</html>
